# ESP-IDF top-level project file.
# Build & run:  idf.py set-target esp32s3 && idf.py flash monitor
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(bpuf_a2k_bench)
